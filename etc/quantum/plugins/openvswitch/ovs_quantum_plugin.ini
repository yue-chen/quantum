[DATABASE]
# This line MUST be changed to actually run the plugin.
# Example: sql_connection = mysql://root:nova@127.0.0.1:3336/ovs_quantum
sql_connection = sqlite://

[OVS]
integration-bridge = br-int
plugin_driver = quantum.plugins.openvswitch.ovs_quantum_plugin.VlanMap
agent_driver = OVSQuantumAgent
# plugin_driver = quantum.plugins.openvswitch.ryu.plugin_driver.OFPRyuDriver
# agent_driver = OVSQuantumOFPRyuAgent

# openflow-controller = <host IP address of ofp controller>:<port: 6633>
# openflow-rest-api = <host IP address of ofp rest api service>:<port: 8080>
